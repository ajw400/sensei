<%= simple_form_for(@subcategory, url: lessons_path, method: :post) do |f| %>
          <div class="form-inputs">
            <%= f.input :category, collection: Category.all %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Search" %>
          </div>
        <% end %>
